bot/config.yaml
bot/state/
bot/downloads/
# Legacy single-file state (superseded by bot/state/); ignored if still present.
bot/telegram-cache.json
bot/telegram-state.json
wrapper/wrapper-data/
*.zip
.env
docker-compose.override.yml
context.md
# Per-instance AppleMusicDecrypt login configs (contain no secrets, but are host-specific)
.logins/wm-*.toml
.logins/login-*.log
.logins/*.nohup.log

# graphify knowledge-graph output (local cache, regenerated by /graphify)
graphify-out/
docs/tgupload.py
